# XXGate

XXGate 托管上游模型账户，为调用方提供可排队、可追踪的请求转发。以下术语区分调用方身份、上游身份、账户状态和计量事实。

## Language

**上游账户 (Upstream Account)**：
被网关托管、属于某个上游提供方和接入方式的账户，具有独立的授权、额度和启用状态；首版为 ChatGPT 账户。
_Avoid_：终端用户、管理员、网关 Key

**入站协议 (Ingress Protocol)**：
调用方与网关交换请求、响应及身份信息的约定，不决定实际使用哪一家上游服务。
_Avoid_：模型厂商、账户类型、出站客户端配置

**上游提供方 (Provider)**：
实际接收模型调用并提供模型能力的服务方，与调用方使用的入站协议分开识别。
_Avoid_：入站客户端、模型名称、网关 Key

**接入方式 (Access Kind)**：
访问上游服务所使用的产品通道及授权约定，例如订阅账户通道与官方 API 通道。
_Avoid_：仅凭模型名称判断的通道、入站协议

**模型能力 (Model Capability)**：
模型在指定提供方、接入方式和账户权限下能够处理的输入、输出、工具及服务档位范围。
_Avoid_：所有模型通用能力、仅由客户端参数声明的支持

**管理员 (Administrator)**：
管理账户、接入凭证和运行设置的操作者，不代表使用模型的终端用户。
_Avoid_：上游账户、客户

**网关 Key (Gateway Key)**：
调用方访问网关的凭证，具有自己的会话隔离范围，不代表一个真实用户。
_Avoid_：OpenAI API Key、OAuth Token、用户账户

**路由分组 (Routing Group)**：
限定网关 Key 可使用哪些上游账户的范围。每个 Key 属于一个分组，账户可属于多个分组；路由和迁移都必须留在 Key 当前分组内。已有数据归入默认分组。
_Avoid_：用户权限组、独立额度池、计费套餐

**账户可用性 (Account Eligibility)**：
账户属于请求分组、已启用、支持请求模型、允许客户端来源且未因更新暂时阻止派发。没有可用账户立即拒绝；有可用账户但并发满载时才允许限时排队。
_Avoid_：当前必有空闲并发、无限等待账户恢复

**客户端来源 (Client Origin)**：
根据入站请求的客户端特征识别出的调用方软件类别；来源可以未知，与会话是否可追踪、调用授权及上游提供方分别判断。
_Avoid_：管理员身份、上游客户端配置、模型名称、已认证的软件身份

**账户来源限制 (Account Source Restriction)**：
账户对可接收客户端来源的限制。默认接受所有来源；仅允许 Codex 时，该账户只接收被识别为 Codex 的请求。
_Avoid_：全局客户端白名单、账户启用状态、会话标识必填

**客户端会话 (Client Session)**：
调用方声明的一组连续对话及其关联线程，是账户粘连的单位。
_Avoid_：HTTP 连接、客户端安装、真实用户

**独立请求 (Stateless Request)**：
没有声明可识别客户端会话、但具有合法独立调用范围的请求。不承诺与其他请求的会话连续性，仍可具有实际缓存用量。
_Avoid_：匿名鉴权、共享 Key 会话、缓存禁用、无法调用

**客户端线程 (Client Thread)**：
客户端会话中的一条具体对话执行线，根线程与子线程可以属于同一会话。同一线程串行执行，不同线程可在会话并发上限内并行。
_Avoid_：上游账户、单次请求

**会话并发上限 (Session Concurrency Limit)**：
同一网关 Key 下，一个客户端会话及其全部线程可同时占用的派发槽数量；与账户和全局并发上限同时生效，等待不占槽。它限制单个任务的并行占用，不代表一个真实用户的总并发。
_Avoid_：账户容量、用户限流、每个子线程各自独立的配额

**绑定代次 (Binding Generation)**：
一个客户端会话在某次账户分配期间使用的上游身份范围，每次账户迁移产生新代次。
_Avoid_：永久账户绑定、重试次数、客户端会话 ID

**身份映射 (Identity Mapping)**：
客户端标识与某一绑定代次内上游标识的对应关系，包含标识之间的父子和配对关系。
_Avoid_：用户画像、全局字符串替换

**账户粘连 (Account Affinity)**：
会话在账户仍启用期间持续使用该账户的关系，并发满不解除这项关系。
_Avoid_：永不迁移、按用户固定账户

**账户迁移 (Account Migration)**：
原账户停用后，同一客户端会话在同一提供方的兼容接入范围内分配到其他账户并建立新身份范围的过程。
_Avoid_：失败重试、请求重放、负载轮转、跨厂商上下文转换

**上游尝试 (Upstream Attempt)**：
一次入站请求实际发往官方的一次模型调用；未派发请求没有上游尝试。
_Avoid_：OAuth 刷新、额度查询、客户端重新请求

**加密推理恢复 (Encrypted Reasoning Recovery)**：
上游明确拒绝加密推理状态后，舍弃该状态并在原账户、原绑定内再尝试一次的受限恢复；丢弃的推理状态不再属于本次上下文。
_Avoid_：账户迁移、无损上下文恢复、通用失败重试、恢复已开始的生成

**停用原因 (Disable Reason)**：
关闭账户启用开关时记录的原因，用于解释状态变化，不是独立的恢复条件。
_Avoid_：待自动恢复状态、多重启用锁

**额度快照 (Quota Snapshot)**：
某一时刻上游报告的账户额度窗口与使用情况，不等于网关记录的 Token 用量。
_Avoid_：本地余额、人民币金额、精确单请求额度消耗

**用量事实 (Usage Fact)**：
一次上游调用所报告或能够明确确认的模型使用量，包含来源和完整性。
_Avoid_：估算费用、账户额度百分比

**人民币计价 (CNY Valuation)**：
按指定价格版本对用量事实计算出的人民币金额，不代表终端用户扣款或官方账单。
_Avoid_：余额扣减、官方实际成本、汇率换算

**客户端配置 (Client Profile)**：
账户出站请求采用的稳定兼容特征，不包含把独立真实会话合并成一个会话的含义。
_Avoid_：真实用户身份、不可检测保证
